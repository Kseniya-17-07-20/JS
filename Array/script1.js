let arr = ["Hello"];
let arr1 = [];
    for(let i = 0; i < arr1.length; i++){
        arr1[i] = arr[i].length;
    }
    console.log('Array with strings', arr);
    console.log('Array with strings', arr1);
